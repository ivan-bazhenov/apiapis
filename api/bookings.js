module.exports = function(params) {
  var module = {};

  /*
  GET /bookings/{bookingNumber}/{departureDate}
   Returns list of booking references or direct login URLs to a PaxRelax site.
   */
  module.get = function(bono, depdate) {
//    todo:
  };

  return module;
};
